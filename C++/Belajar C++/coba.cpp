#include <iostream>
#include <string>
using namespace std;

int main(){
int a, b;
cin >> a >> b;
string output;
output = (a > b) ? "Nilai " << a << "lebih besar dari nilai " << b : "Nilai " << b << "lebih besar dair nilai " << a;
}
